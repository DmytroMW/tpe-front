import ContainerWrapper from '../components/ContainerWrapper';

const Activation = () => {
  return (
    <>
      <ContainerWrapper title={ 'Activation page' } />
    </>
  );
};

export default Activation;